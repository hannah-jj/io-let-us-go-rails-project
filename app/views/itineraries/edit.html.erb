<div class="col-lg-6">
	<div class="well bs-component">

			<fieldset>
				<legend> Edit Itinerary to: <%=@event.title.capitalize%></legend>
					<%= render partial: 'itineraries/form', locals: {itinerary: @itinerary, event: @event } %>
			</fieldset>

	</div>
</div>