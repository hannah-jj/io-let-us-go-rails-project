<table class="table table-striped table-hover">
	<tbody class="comments-table">
		{{#if comments}}
			{{#each comments}}
			  <tr class="info">
			    <td>{{note}}</td>
			    <td>{{email}}</td>
			    <td>{{created-at}}</td>
			    <td align="right"><a class="btn-info" href="/comments/{{id}}">view</td>
			  </tr>
			{{/each}}
		{{else}}
			<tr class="no-comment"><td>no comments yet</td></tr>
		{{/if}}
	</tbody>
</table>

