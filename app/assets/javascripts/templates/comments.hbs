<input type="text" id="new-comment">
<button class="add-comment btn-info">Add Comment</button>
<table class="table table-striped table-hover">
	<tbody class="comments-table">
		{{#if comments}}
			{{#each comments}}
			  <tr class="info">
			    <td>{{attributes.note}}</td>
			    <td>{{attributes.email}}</td>
			    <td><a class="btn btn-info" href="/events/{{attributes.event-id}}/comments/{{id}}">view</td>
			  </tr>
			{{/each}}
		{{else}}
			<tr class="no-comment"><td>no comments yet</td></tr>
		{{/if}}
	</tbody>
</table>

